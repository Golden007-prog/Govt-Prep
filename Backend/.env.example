# Copy to `.env` (gitignored). The local backend uses your Claude SUBSCRIPTION via the
# claude CLI — no API key. Blank = your interactive `claude` login session is used.
# For headless/token auth: run `claude setup-token` and paste the result.
CLAUDE_CODE_OAUTH_TOKEN=
